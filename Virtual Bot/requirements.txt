fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
APScheduler>=3.10,<4
python-multipart>=0.0.9
pyyaml>=6.0
# >=14: параметр connect(..., proxy=None) — міст до дисплея вимикає проксі
websockets>=14.0
PyJWT[crypto]>=2.8
# Локальний open-source ASR для української; модель завантажується ліниво.
faster-whisper>=1.1.0,<2
# Музика на екрані (Now Playing): неофіційний YouTube через yt-dlp +
# безкоштовний транскрайб субтитрів без ключа. Опціональні в коді: без них
# музика/транскрайб чесно віддають 503, решта бота працює.
yt-dlp>=2025.1.15
youtube-transcript-api>=1.0
