fastapi
uvicorn[standard]
# >=5.0 dropped cv2.CascadeClassifier / cv2.data.haarcascades (Haar Cascade
# API) in favor of DNN-based FaceDetectorYN — pin <5 so detect_faces() works.
opencv-python<5
python-multipart
pyyaml
requests
